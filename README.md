# STL_practice_pgm.cpp
Set Search — Checks whether a given number exists in a set. Set Difference — Finds the elements that are present in one set but not in the other. Mode Finder — Finds the element that appears most frequently in an array. Palindrome Checker — Checks whether a string reads the same forward and backward. Josephus Survivor.
